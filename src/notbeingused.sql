create if not exists database test1

create table tabletest (
    test1 varchar(50)
    test2 varchar(50)
    test3 varchar(50)
    test4 varchar(50)
);

CREATE TABLE Persons (
    PersonID int,
    LastName varchar(255),
    FirstName varchar(255),
    "address" varchar(255),
    City varchar(255)
);